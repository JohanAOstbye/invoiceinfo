import { MarkdownOptions } from 'markdown-to-jsx';
interface MarkdownProps {
    children: string;
    options?: MarkdownOptions;
}
export declare const Markdown: ({ children, options }: MarkdownProps) => JSX.Element;
export declare const md: (literals: TemplateStringsArray, ...placeholders: string[]) => JSX.Element;
export {};
