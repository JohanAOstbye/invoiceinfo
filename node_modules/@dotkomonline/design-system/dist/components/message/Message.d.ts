import * as React from 'react';
interface MessageProps {
    status: 'info' | 'success' | 'warning' | 'error';
    children: React.ReactNode;
}
declare const Message: ({ status, children }: MessageProps) => JSX.Element;
export default Message;
