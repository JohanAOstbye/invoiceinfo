import { StyledComponentProps } from 'styled-components';
interface SelectProps extends StyledComponentProps<'select', any, any, any> {
    options: [{
        value: any;
        label: any;
    }];
}
declare const Select: ({ options, ...rest }: SelectProps) => JSX.Element;
export default Select;
