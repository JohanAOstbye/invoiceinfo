import React from 'react';
interface Props {
    open: boolean;
    onClose: () => void;
    children: React.ReactNode;
}
declare const Modal: ({ open, onClose, children }: Props) => JSX.Element;
export default Modal;
