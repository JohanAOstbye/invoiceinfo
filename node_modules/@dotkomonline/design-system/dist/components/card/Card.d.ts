import * as React from 'react';
interface CardProps {
    children: React.ReactNode;
    elevated?: boolean;
    padding?: boolean;
    [name: string]: any;
}
declare const Card: ({ children, elevated, padding, ...props }: CardProps) => JSX.Element;
export default Card;
